const activityTypeEnum = ['Garbage sorting', 'Paper waste sale', 'Resource saving tech',
    'Plant a piece of flora', 'Clean a patch of land']
const statusEnum = ['pending', 'testing','rejected', 'accepted']

module.exports = {
    activityTypeEnum,
    statusEnum
}